---
description: List your backchannel rooms and whether each is still live
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/backchannel:*)
---
Show the user their backchannel rooms and each room's live status, verbatim.

!`"${CLAUDE_PLUGIN_ROOT}/bin/backchannel" status`
