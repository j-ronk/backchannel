---
description: Check backchannel setup (Node, relay, command-sandbox grants) and print any fixes
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/backchannel:*)
---
Run the backchannel doctor and show its output to the user verbatim.

!`"${CLAUDE_PLUGIN_ROOT}/bin/backchannel" doctor`
