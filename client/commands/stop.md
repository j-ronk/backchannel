---
description: Leave or close the backchannel room
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/backchannel:*)
---
End the shared session and confirm to the user.

!`"${CLAUDE_PLUGIN_ROOT}/bin/backchannel" stop`
